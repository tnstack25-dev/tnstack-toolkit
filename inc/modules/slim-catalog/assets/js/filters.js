(function(){'use strict';function init(root){var form=root.querySelector('form'),result=root.querySelector('.sc-filter-result'),more=root.querySelector('[data-load-more]'),page=1,busy=false;function load(append){if(busy)return;busy=true;root.classList.add('is-loading');var data=new FormData(form);data.append('action','slim_catalog_filter');data.append('nonce',SlimCatalogFilters.nonce);data.append('page',page);data.append('per_page',root.dataset.perPage);fetch(SlimCatalogFilters.ajaxUrl,{method:'POST',credentials:'same-origin',body:data}).then(function(r){return r.json();}).then(function(r){if(!r.success)throw new Error();var html='<div class="sc-grid" style="--sc-columns:'+root.dataset.columns+'">'+r.data.html+'</div>';if(append){var holder=document.createElement('div');holder.innerHTML=r.data.html;holder.querySelectorAll('.sc-card').forEach(function(card){result.querySelector('.sc-grid').appendChild(card);});}else result.innerHTML=html;more.hidden=!r.data.has_more;window.dispatchEvent(new Event('resize'));}).catch(function(){result.innerHTML='<p class="sc-empty">Không thể tải sản phẩm. Vui lòng thử lại.</p>';more.hidden=true;}).finally(function(){busy=false;root.classList.remove('is-loading');});}form.addEventListener('submit',function(e){e.preventDefault();page=1;load(false);});form.querySelectorAll('select').forEach(function(el){el.addEventListener('change',function(){page=1;load(false);});});more.addEventListener('click',function(){page++;load(true);});load(false);}document.addEventListener('DOMContentLoaded',function(){document.querySelectorAll('[data-sc-filter]').forEach(init);});})();
